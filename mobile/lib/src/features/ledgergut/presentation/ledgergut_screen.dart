import 'package:flutter/material.dart';

class LedgergutScreen extends StatelessWidget {
  const LedgergutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ledgergut service scaffold (no business logic yet).'),
      ),
    );
  }
}
